# Testing Rules

- Write tests for new features
- Place test files next to source: `*.test.ts`
- Test names describe "when X, should Y"
- Only mock external dependencies (API, DB). Never mock internal logic
- Coverage target: 80%+
